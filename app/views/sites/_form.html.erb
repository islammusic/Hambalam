<% form_for @site do |f| %>
  <p>
    <%= f.label :ime %>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :include %>
    <%= f.text_area :include %>
  </p>

  <%= f.submit "Shrani" %>
<% end %>